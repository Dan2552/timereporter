(function(){
  var $time_slot = $('*[data-datetime="<%= @time_entry.entry_datetime %>"]').addClass('chosen'),
      $entry = $time_slot.find('.entry').attr('data-id', "<%= @time_entry.id %>").toggleClass('chosen new_entry');

  time_entries.set_resizeable( $entry );

})();