(function() {
  var $entry = time_entries.$entry,
      $new_entry = $("<%=j render(@time_entry) %>"),
      $form = $('<%=j render "form" %>').hide();

  $entry.replaceWith($new_entry);
  time_entries.$entry = $new_entry;
  time_entries.show_form($form);

})();