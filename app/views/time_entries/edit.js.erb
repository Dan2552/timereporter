(function() {
  var $entry = $("#time_entry_<%= @time_entry.id %>"),
    $form = $('<%=j render "form" %>').addClass("<%= @time_entry.project.present? ? 'edit-form' : '' %>");

  timeEntries.showForm($form);

})();
